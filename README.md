# Fetch Posts Using JavaScript (JSONPlaceholder)

This project is a simple HTML + JavaScript example that fetches data from a public API and displays it on a web page.

## 📌 Description

- Uses the **JSONPlaceholder** fake REST API
- Fetches a list of posts using JavaScript `fetch()`
- Displays each post as JSON on the web page
- No frameworks required (pure HTML & JavaScript)

## 📂 Project Structure

```
index.html
README.md
```

## ▶️ How to Run

### Option 1: Open directly in browser
1. Download or clone this repository
2. Open `index.html` in any modern browser (Chrome, Firefox, Edge)

### Option 2: Using Live Server (recommended)
1. Open the folder in VS Code
2. Install **Live Server** extension
3. Right-click `index.html` → **Open with Live Server**

## 🌐 API Used

```
https://jsonplaceholder.typicode.com/posts
```

This API returns a list of sample post objects.

## 🧠 Key Concepts Used

- HTML DOM manipulation
- JavaScript `fetch()` API
- Promises (`then`, `catch`)
- `insertAdjacentHTML()`

## 📸 Output

The page displays all post objects fetched from the API.

## 🛠️ Technologies

- HTML5
- JavaScript (ES6)

---

Author: Gopi Chand Duriseti
